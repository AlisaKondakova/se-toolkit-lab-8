[project]
name = "edu-assistant"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
    "httpx>=0.27.0",
]

[project.scripts]
nanobot-entrypoint = "entrypoint:main"
